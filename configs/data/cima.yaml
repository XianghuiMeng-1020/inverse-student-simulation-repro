name: cima
source_url: https://aclanthology.org/2020.bea-1.5.pdf
license: public
raw_dir: ${paths.raw}/cima
processed_dir: ${paths.processed}/cima
splits_file: ${paths.splits}/cima.json

primary: false
language: it
subject: italian_grammar
n_dialogues_expected: 391

split_ratios:
  train: 0.70
  dev: 0.10
  test: 0.20

stratify_by: action_type

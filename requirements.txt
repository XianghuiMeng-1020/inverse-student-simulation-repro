# ============================================================
# Inverse Student Simulation — reproducibility requirements
# Python 3.11 or 3.12
# ============================================================
# Core ML stack
torch>=2.6.0
transformers>=4.46.0,<5.0
tokenizers>=0.20
accelerate>=1.0
peft>=0.13
bitsandbytes>=0.46
datasets>=3.0
safetensors>=0.4
huggingface-hub>=0.26

# Retrieval / embeddings (misconception rerank, auxiliary benchmarks)
sentence-transformers>=3.0

# API clients (silver labeling, E5/E7 baselines)
openai>=1.50
httpx>=0.27
python-dotenv>=1.0

# Config / experiment tracking
hydra-core>=1.3
omegaconf>=2.3
wandb>=0.18

# Data / numerics
numpy>=1.26,<2.2
pandas>=2.2
pyarrow>=17.0
scipy>=1.13
scikit-learn>=1.5

# Schemas / validation
pydantic>=2.9
jsonschema>=4.23

# Plotting (E1 extended reliability diagrams)
matplotlib>=3.9

# Utilities
tqdm>=4.66
rich>=13.7
typer>=0.12
loguru>=0.7

# Testing
pytest>=8.3
